# Core dependencies
transformers>=4.35.0
accelerate>=0.24.0
sentencepiece>=0.1.99
protobuf>=3.20.0
huggingface-hub>=0.19.0

# PyTorch - Install based on your system:
# For GPU (CUDA 11.8): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For GPU (CUDA 12.1): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU only: pip install torch torchvision torchaudio
# 
# Or install from requirements (CPU only):
# torch>=2.0.0
